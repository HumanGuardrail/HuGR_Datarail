//! `datarail-rail` — the ephemeral, substrate-polymorphic transport (SPEC `07-rail-substrate.md`): it moves
//! cofres using only the authenticated header + seal (`INV-OPAQUE-CARGO`), never the payload. **Stub — P3.**
