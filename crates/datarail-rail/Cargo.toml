[package]
name = "datarail-rail"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "datarail-rail — ephemeral, substrate-polymorphic transport moving cofres by header+seal (SPEC 07)."

[lints]
workspace = true

[dependencies]
datarail-core = { path = "../datarail-core" }
datarail-cofre = { path = "../datarail-cofre" }
