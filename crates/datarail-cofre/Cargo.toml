[package]
name = "datarail-cofre"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "datarail-cofre — canonical Cofre wire encoding + seal/verify (SPEC 02; parse-before-verify, BLK-4/7/8)."

[lints]
workspace = true

[dependencies]
datarail-core = { path = "../datarail-core" }
datarail-crypto = { path = "../datarail-crypto" }
